import XCTest
@testable import PokepediaSpecies

final class PokepediaSpeciesTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PokepediaSpecies().text, "Hello, World!")
    }
}
